---
# tasks file for roles/nginx
- name: Create Nginx container
  docker_container:
    name: nginx
    image: myitam/nginx: ${{ github.run_number }}
    recreate: yes
    ports:
     - "80:80"
    restart: yes
    restart_policy: always